{
  "name": "textlint-rule-preset-japanese-elementary-school",
  "description": "textlint rule preset to lint text for Japanese elementary school students",
  "author": "being-ish, Inc. <dev@being-i.sh>",
  "license": "MIT",
  "keywords": [
    "textlintrule",
    "japanese",
    "elementaryschool"
  ],
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=*",
    "test": "npm run test --workspace=*",
    "pretest": "npm run build",
    "changeset": "changeset",
    "clean": "npm run clean --workspace=*"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.2"
  }
}
